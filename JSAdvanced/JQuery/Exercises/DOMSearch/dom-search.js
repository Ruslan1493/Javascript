function domSearch(content, isSensitive) {
    let divAdd = $('<div>');
    divAdd.addClass('add-controls');
    let labelAdd = $('<label>');
    console.log(labelAdd);
    label.text("enter");
    label.append('<input>');
    divAdd.append(label);
    let a = $('<a>');
    a.addClass('button');
    a.css('display', 'inline-block');
    a.text(Add);

    content.append(divAdd);
}
